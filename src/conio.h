void cooked(void);   //used in kdhit function and raw
void raw(void);   //used in kbhit function
int kbhit(void);   //kbhit - Determines if a keyboard key was pressed.
int getch(void);   //getch - Reads a character directly from the console without buffer, and without echo.
